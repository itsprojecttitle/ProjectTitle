# Codex Session Rules

- Number updates as "Update #N" in responses when repo changes are made.
- Do not number changes; only number repo updates.
- When prompted to "Undo" or "Redo", undo/redo the current numbered update.
- Reset the numbering when the user says "refresh".
